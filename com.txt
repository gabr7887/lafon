form div{
  display: grid;
  gap:4px;
}

form{
  display: grid;
  gap: 8px;
}

form input,
form textarea,
form select{
  background-color: var(--white);
  padding: .8rem;
  border: 1px solid #333;
  border-radius: 5px;
  color: var(--text-color);
}

form div{
  max-width: 100%;
}

label, form input,
form textarea,
form select, .error {
  font-size: 1rem;
  font-family: 'Montserrat', sans-serif;
  line-height: 1.6;
}

.dark-mode input, .dark-mode #service, .dark-mode #detalhes{
  border: 1px solid #ddd;
}

form button{
  max-width: fit-content;
  font-size: 1.125rem;
}



form input:focus,
#detalhes:focus,
#service:focus {
  outline: none;
  border-color: var(--primary-color);
}










/* form input,
form textarea,
form select {
  width: 100%;
  padding: 0.8rem;
  margin-bottom: 1rem;
  border: 1px solid #ddd;
  border-radius: 5px;
  transition: border-color 0.3s ease, background-color 0.3s ease, color 0.3s ease;
  background-color: var(--white);
  color: var(--text-color);
}

form input:focus,
form textarea:focus,
form select:focus {
  outline: none;
  border-color: var(--primary-color);
}


form textarea {
  resize: none;
  height: 150px;
  font-family: 'Montserrat', sans-serif;
  line-height: 1.6;
  
}

label{
  display: block;
}

*/
